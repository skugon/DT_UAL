import { SeatTypes } from '../../core/constants.js';
export const hasEditorSeat = (loaders) => async ({ userId, workspaceId }) => {
    const seat = await loaders.getWorkspaceSeat({
        userId,
        workspaceId
    });
    if (!seat)
        return false;
    return seat === SeatTypes.Editor;
};
//# sourceMappingURL=workspaceSeat.js.map