export const isDashboardOwner = (loaders) => async ({ userId, dashboardId }) => {
    const dashboard = await loaders.getDashboard({ dashboardId });
    if (!dashboard)
        return false;
    return dashboard.ownerId === userId;
};
//# sourceMappingURL=dashboards.js.map