export const toGraphqlResult = (authResult) => {
    if (authResult.isOk) {
        return {
            authorized: true,
            code: 'OK',
            message: 'OK',
            payload: null
        };
    }
    else {
        const error = authResult.error;
        return {
            authorized: false,
            code: error.code,
            message: error.message,
            payload: (error.payload || null)
        };
    }
};
//# sourceMappingURL=graphql.js.map