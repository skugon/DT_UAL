import { ensureCanAccessSavedViewFragment, WriteTypes } from '../../../fragments/savedViews.js';
export const canEditSavedViewTitlePolicy = (loaders) => async ({ userId, projectId, savedViewId }) => {
    return await ensureCanAccessSavedViewFragment(loaders)({
        userId,
        projectId,
        savedViewId,
        access: WriteTypes.EditTitle
    });
};
//# sourceMappingURL=canEditTitle.js.map