import { ensureCanAccessSavedViewFragment, WriteTypes } from '../../../fragments/savedViews.js';
export const canEditSavedViewDescriptionPolicy = (loaders) => async ({ userId, projectId, savedViewId }) => {
    return await ensureCanAccessSavedViewFragment(loaders)({
        userId,
        projectId,
        savedViewId,
        access: WriteTypes.EditDescription
    });
};
//# sourceMappingURL=canEditDescription.js.map