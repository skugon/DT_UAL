import { ensureCanAccessSavedViewGroupFragment } from '../../../fragments/savedViews.js';
export const canUpdateSavedViewGroupPolicy = (loaders) => async ({ userId, projectId, savedViewGroupId }) => {
    return await ensureCanAccessSavedViewGroupFragment(loaders)({
        userId,
        projectId,
        savedViewGroupId,
        access: 'write'
    });
};
//# sourceMappingURL=canUpdateGroup.js.map