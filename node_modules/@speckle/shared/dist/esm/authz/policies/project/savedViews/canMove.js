import { ensureCanAccessSavedViewFragment, WriteTypes } from '../../../fragments/savedViews.js';
export const canMoveSavedViewPolicy = (loaders) => async ({ userId, projectId, savedViewId }) => {
    return await ensureCanAccessSavedViewFragment(loaders)({
        userId,
        projectId,
        savedViewId,
        access: WriteTypes.MoveView
    });
};
//# sourceMappingURL=canMove.js.map