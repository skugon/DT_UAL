import { ensureCanAccessSavedViewFragment, WriteTypes } from '../../../fragments/savedViews.js';
export const canUpdateSavedViewPolicy = (loaders) => async ({ userId, projectId, savedViewId }) => {
    return await ensureCanAccessSavedViewFragment(loaders)({
        userId,
        projectId,
        savedViewId,
        access: WriteTypes.UpdateGeneral
    });
};
//# sourceMappingURL=canUpdate.js.map