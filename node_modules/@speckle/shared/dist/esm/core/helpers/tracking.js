import { md5 } from '../utils/md5.js';
export function resolveMixpanelUserId(email) {
    return '@' + md5(email.toLowerCase()).toUpperCase();
}
export function resolveMixpanelServerId(serverHostname) {
    return md5(serverHostname.toLowerCase()).toUpperCase();
}
export const resolveDistinctId = resolveMixpanelUserId;
//# sourceMappingURL=tracking.js.map