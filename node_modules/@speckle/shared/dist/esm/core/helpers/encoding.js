// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const safeParse = (json, guard) => {
    try {
        const parsed = JSON.parse(json);
        return guard ? (guard(parsed) ? parsed : null) : parsed;
    }
    catch {
        return null;
    }
};
//# sourceMappingURL=encoding.js.map