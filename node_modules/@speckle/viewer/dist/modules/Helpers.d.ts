import { Box3, Matrix4, Vector2 } from 'three';
export declare function getRelativeOffset(referenceBox: Box3, offsetAmount?: number): number;
export declare function makePerspectiveProjection(screenSize: Vector2, fov: number, near: number, far: number): Matrix4;
