import { BufferGeometry, Camera, Material, MeshMatcapMaterialParameters, Object3D, Scene, type IUniform } from 'three';
import { ExtendedMatcapMaterial, type Uniforms } from './SpeckleMaterial.js';
import type { SpeckleWebGLRenderer } from '../objects/SpeckleWebGLRenderer.js';
declare class SpeckleMatcapMaterial extends ExtendedMatcapMaterial {
    protected get vertexProgram(): string;
    protected get fragmentProgram(): string;
    protected get baseUniforms(): {
        [uniform: string]: IUniform;
    };
    protected get uniformsDef(): Uniforms;
    constructor(parameters: MeshMatcapMaterialParameters, defines?: never[]);
    /** We need a unique key per program */
    customProgramCacheKey(): string;
    copy(source: Material): this;
    fastCopy(from: Material, to: Material): void;
    /** Called by three.js render loop */
    onBeforeRender(_this: SpeckleWebGLRenderer, _scene: Scene, _camera: Camera, _geometry: BufferGeometry, object: Object3D): void;
}
export default SpeckleMatcapMaterial;
